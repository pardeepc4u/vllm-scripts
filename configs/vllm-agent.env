# ============================================================================
# Agent: Merged into vllm-chat-agent.service
# Agent is now served by Qwen 3.5 27B on port 8001
# ============================================================================

# To use agent, call the chat endpoint with model name "agent"
# Example:
#   client.chat.completions.create(
#       model="agent",
#       messages=[...],
#       tools=[...]
#   )
